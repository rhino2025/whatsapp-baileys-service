# Shared bearer token between HostStay and this transport.
# Use the SAME value as WHATSAPP_RAILWAY_TOKEN in Lovable Cloud secrets.
RAILWAY_TOKEN=replace-me

# Where to mount Baileys auth state (mount a Railway volume at this path).
AUTH_DIR=/data/auth

# Optional override
PORT=8080
